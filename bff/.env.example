# API keys are NOT stored here. Set them as Windows user environment variables (setx):
#   ANTHROPIC_API_KEY, GOOGLE_AI_API_KEY   (loaded from process env)
AI_MOCK_MODE=true
BFF_PORT=4000
KNOWLEDGE_URL=http://localhost:8000
