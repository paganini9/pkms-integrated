# 릴리스 노트 — g3-release (Phase 3 완료)

> PKMS 통합(와이퍼 지식·온톨로지) · Phase 3(검증·패키징) 완료. 게이트 판정 = **실 스택·Solar 실키**.
> 작성 2026-07-11 · 브랜치 develop.

## 1. 오케스트레이터 §5 통합 게이트 — 전 항목 통과

| 항목 | 결과 | 근거 |
|------|:----:|------|
| **추적성** | ✅ | SC-1~4·6~8 → FR-01~16 → 구현 → 테스트. 회귀셋이 AC-1~8 을 FR 로 태깅(`regression_set.jsonl`). |
| **계약 준수** | ✅ | `validate_contracts.py` 스키마 6 · 검증 27건 통과. 각 레이어 입출력이 `contracts/` 일치. |
| **경계** | ✅ | 외부는 BFF `/api/v1` 만. 지식·추론·RAG 내부 비노출(T-55 no-leak 테스트, 블랭크노드/트리플 미유출). |
| **안전** | ✅ | 진입 가드레일 결정론 접지(T-73) — **우회 8종 Solar 9/9 차단**. 근거부족 시 "명세 근거 없음" fail-closed. |
| **신뢰성** | ✅ | Timeout·Retry·CircuitBreaker(`reliability.test.ts`) + **Idempotency 영속(T-83 draft_id)** — ≥2 충족. |
| **재현성** | ✅ | 추출 structured(json_object) · 결정론 검증(satisfy·SHACL·reasoner) 같은 입력→같은 판정. **owlrl↔HermiT 패리티 일치**(T-80). |
| **QA** | ✅ | 회귀 **24 PASS**(Solar 실키) · 환각 0(가드레일) · 출처 부착(근거성) · 미검증근거 0(계약상 verified 만). |
| **패키징** | ✅ | 로컬=Docker. compose 기동 `_bootstrap` 이 시드 322 트리플 + 6문장 벡터 인덱싱, `/health.reasoner=ok`(JRE). |

**운영 provider(Solar) 실키 검증**: 회귀셋·LLM-judge 루브릭·실패 케이스 전부 **Solar 실 스택**에서 통과.

## 2. Phase 3 산출 (완료·푸시)

- **T-87** 시드 inferred 엣지(AC-4 점선 렌더) · **T-70** 회귀셋+실 스택 러너 · **T-88** Solar 운영 provider
- **가드** 컬렉션-임베더 일치 · **T-83** draft_id 영속 멱등 · **T-84** 상태 외부화 검토
- **T-73** 결정론 접지(가드레일을 프롬프트 규율에서 분리) · **T-71** LLM-judge · **T-72** 실패 케이스
- **T-80** Dockerfile×3+compose·HermiT 첫 실행 · **T-85** 상위 온톨로지 TTL 영속화
- **T-81** compose 완성(볼륨·healthcheck·시드적재·모델 bake) · **T-82** CI 2계층 · **T-86** flaky 격리

## 3. 이번 페이즈가 값을 치르고 얻은 결정

1. **solar-pro3 는 `json_schema strict` 로 품질이 떨어진다**(정답 A→C·추출 공집합) → `json_object`+프롬프트-스키마.
2. **가드레일은 프롬프트 규율이 아니라 결정론 온톨로지 소속(T-73)으로 접지**해야 한다 — 프롬프트 완화 뒤에도 우회 9/9 차단.
3. **LLM 심판(solar-pro3)은 비결정적**(같은 답변 grounded 2/3→1/3) → 하드 게이트는 결정론 루브릭, LLM 심판은 자문.
4. **HermiT 경로를 처음 실행**(호스트 no_jre 로 미검증이던 `_consistency_hermit`) — Docker JRE 로 seed 일관·모순 검출·owlrl 패리티 확인.
5. `/health` 가 provider 를 오보하던 잠복 버그 수정(실제 게이트웨이 선택 반영).

## 4. 테스트·게이트 수치

- 지식 pytest **104 통과**·4 skip(호스트 no_jre — HermiT 는 컨테이너에서 실행) · BFF vitest **49** · frontend build OK
- 계약 검증 **27** · 회귀셋 Solar **24 PASS**·1 skip(meta) · 루브릭 0 위반 · 실패 케이스 0 위반
- HermiT 컨테이너: reasoner=ok · seed consistent · 모순 clash 검출 · **owlrl↔HermiT 패리티 일치**

## 5. 알려진 한계 (릴리스 비차단)

- **solar-pro3 추출 변동성**: in-domain 문장에서 간헐적 공집합/과추출. 접지가 안전하게 fail-closed(환각 아님). 러너는 재시도로 흡수.
- **mock 임베딩 리콜**: 자유질의(RAG) 리콜은 `EMBEDDING_PROVIDER=local`(full 이미지)에서 본연. mock 은 안전한 insufficient.
- **full(ML) 이미지 bake**: Dockerfile 로직 구현. 대용량이라 이 세션에선 미빌드(경량 경로 전부 실측).
- **CI**: GitHub 러너 실행은 원격. 명령은 로컬과 동일하게 미러·검증.

## 6. 백로그 (포스트-g3, 릴리스 비차단)

- **T-89** 저작 OOV 트리아지 + SKOS 어휘층 + 제안 큐 MVP(`docs/OOV-용어처리-온톨로지진화-설계제안.md`). 접지 fail-closed 불변. g3-release 후 착수 여부 결정.

---
**태그: `g3-release`** — Phase 3 완료. 다음은 T-89 착수 여부 결정.
