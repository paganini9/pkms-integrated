# 작업 분해 · 의존성 맵 (병렬화 계획)

> 오케스트레이터가 `task_board.md` 초기화 시 사용하는 기준. DAG·게이트·병렬 묶음 정의.

## 1. 의존성 DAG

```
        ┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
        │ 01 아키텍처·표준                                                                                 │
        │   → 레포 골격 + contracts/{api_standard, interface_contracts,                                    │
        │     error_model, state_schema, mocks} freeze                                                     │
        └───────────────┬──────────────────────────────────────────────────────────────────────────────────┘
                        │ [게이트 G0: 계약 freeze]
        ┌───────────────┴───────────────── Phase 1 (병렬) ─────────────────────────────────────────────────┐
        │ 02 데이터        03 RAG          06 퍼시스턴스(DB·FS)                                            │
        │ (data/)          (rag/)          (store/ + 스키마)                                               │
        │        04 추론·코어 (reasoning/: satisfy·규칙컴파일러·reasoner·SHACL) ── data/rag는 mock으로 선행│
        └───────────────┬──────────────────────────────────────────────────────────────────────────────────┘
                        │ [게이트 G1: 레이어 인터페이스 적합성]
        ┌───────────────┴───────────────── Phase 2 (통합) ─────────────────────────────────────────────────┐
        │ 05 백엔드·API (BFF: /extraction SSE·/satisfy·/qa·/graph, 04+06 결합)                             │
        │        → 07 프론트엔드 (API 소비; mock API로 선행 가능)                                          │
        └───────────────┬──────────────────────────────────────────────────────────────────────────────────┘
                        │ [게이트 G2: E2E 시나리오 동작]
        ┌───────────────┴───────────────── Phase 3 (검증·패키징) ──────────────────────────────────────────┐
        │ 08 QA(상시·게이트)        09 DevOps(Docker·패리티)                                               │
        └──────────────────────────────────────────────────────────────────────────────────────────────────┘
```

## 2. 게이트 정의
- **G0 계약 freeze**: API 표준·인터페이스 계약·에러 모델·State 스키마·mock fixtures 확정 → Phase 1 착수 허용.
- **G1 인터페이스 적합성**: 02·03·04·06 산출물이 계약 스키마 검증 통과(실/모의 무관).
- **G2 E2E**: SC-1 지식입력→SC-2 satisfy→SC-3 Q&A/환각비교가 실제 BFF로 프론트에서 끝까지 동작(추출 SSE·출처 부착).
- **릴리스**: 오케스트레이터 §5 통합 게이트 전 항목 통과.

## 3. 병렬 묶음 & 모의 전략
| 병렬 묶음 | Agent | 의존(미완 시 mock) |
|---|---|---|
| P1-A | 02 데이터 | 계약만 |
| P1-B | 03 RAG | 계약 + 시드 TTL(6문장) |
| P1-C | 06 퍼시스턴스 | 계약만 |
| P1-D | 04 추론·코어 | data/rag/store **mock** (계약 준수) → 실구현 교체 |
| P2-A | 05 백엔드 | 04 추론 + 06 스토어(실) |
| P2-B | 07 프론트 | 05 API(없으면 mock API 서버) |

## 4. 대표 작업(task) — PKMS 실제 매핑 (상세·최신은 task_board.md 권위)
- T-01 레포 골격 frontend/bff/knowledge + core (01) · T-02 contracts freeze → **G0** (01)
- T-10 시드 TTL 적재·정규화 (02) · T-11 6문장·규칙·카테고리 일관성 (02)
- T-20 임베딩(로컬 ST/MOCK)+Chroma (03) · T-21 하이브리드 검색+충분성 (03) · T-22 카테고리·프로젝트 지식선택 (03)
- T-30 satisfy 엔진(3단계) (04) · T-31 규칙 컴파일러(문장→규칙→SHACL) (04) · T-32 /validate/shacl (04) · T-33 reasoner HermiT+owlrl폴백 (04) · T-34 satisfy 시그니처 캐시 (04)
- T-40 Oxigraph 영속+/sparql(읽기) (06) · T-41 kg/save·delete 트리플+벡터 원자성 (06) · T-42 프로젝트·요구·지식범위 CRUD (06)
- T-55 내부 엔드포인트 /kg/lookup·/graph·/dashboard·/governance/*·/upper-ontology/*·GET /rules (04·06) → **G1**
- T-50 AI Gateway(Solar/Mock/Claude/Gemini)+Timeout·Retry·Breaker (05) · T-51 /extraction stream+validate+save(HITL) (05) · T-52 /satisfy 무변형 (05) · T-53 /qa A/B/C+환각비교 (05) · T-54 /graph·/dashboard·admin(403 게이트) (05)
- T-60 핵심 3화면(지식입력·설계검증·Q&A/환각비교) (07) · T-61 지식맵 Cytoscape(inferred 점선) (07) · T-62 에러UX·amber 승인차단 (07) → **G2**
- T-70~73 QA(회귀셋·수용기준·LLM-judge·unknown_concept) (08·04) · T-80~82 DevOps(Docker·시드적재·CI) (09) → **릴리스**

## 5. 임계 경로(Critical Path)
`01 계약(G0) → 04 satisfy·추론 코어 → 05 BFF API(G2) → 08 QA 게이트 → 09 패키징`.
데이터·RAG·퍼시스턴스·프론트는 임계 경로와 **겹치지 않게 병렬**로 흡수해 총 기간 단축.
