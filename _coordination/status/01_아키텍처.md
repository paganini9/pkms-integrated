# 01 아키텍처·표준 Agent — 상태 보고

> 양식: `agents/공유표준/상태보고_양식.md`. 자기 파일만 수정한다.

## 현재
- 페이즈: **Phase 0 완료 (G0 통과)**
- 진행 task: —

## 완료
- **T-01** 리포 골격 — `frontend/`(Vite+React+TS) · `bff/`(Express+TS) · `knowledge/`(FastAPI)
  - core: 설정(env·시크릿 분리) · 로깅(trace_id·키 마스킹) · 예외 계층 · Protocol · mock 구현
  - 검증: 3서비스 typecheck/import OK · BFF↔지식서비스 health 왕복 · 지식서비스 down 시 `200 degraded`
- **T-02** 계약 v1 freeze — `_coordination/contracts/`
  - `api_standard.md`(BFF 외부 `/api/v1`) · `interface_contracts.md`(내부 HTTP + 레이어 Protocol) · `error_model.md`
  - `schemas/*.json` 6개 · `mocks/*` 15개 + 회귀 시드 · `validate_contracts.py`(기계 검증)
  - 검증: 계약 검증 27건 통과 · 음성 테스트 3건 검출 · Pydantic↔fixture 교차검증 11건 통과

## 계약 결정 (명세 모호함 해소)
- **CD-1** `violations`(정규화) / `violation_bases`(원본 basis) 분리 — AC-2 와 회귀셋 표기 충돌 해소
- **CD-7** `severity:"violation"` = amber + 저장 차단 / `warning` = 정보 + 저장 허용 — AC-1 과 실패케이스표 충돌 해소
- CD-2·3·4·5·6 — `contracts/README.md` §2

## BLOCKER
- 없음

## 인계 사항 (Phase 1 각 Agent 에게)
- **02**: `knowledge/ontology/README.md` 의 CD-3 정규화가 첫 task (`AggravationRule` → `SpringRule`·`ArmRule`)
- **03**: `EMBEDDING_PROVIDER=mock` 이 기본. `sentence-transformers` 는 `requirements-ml.txt`(선택)
- **04**: `knowledge/ontology-ref/` 는 **수정 금지**(참조 원본). 이식 대상은 `reasoning/`
- **06**: `chromadb` 는 1.5.9 로 고정 — 0.6.x 로 내리면 Windows 에서 설치가 깨진다(MSVC 빌드툴 요구)
- **전원**: 의존 레이어가 미완이면 `knowledge/core/mocks.py` 로 진행. 서로 기다리지 않는다.
